rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Grant full read/write permissions to all documents for development synchronization
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
